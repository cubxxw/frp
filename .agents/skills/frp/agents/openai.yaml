interface:
  display_name: "Frp"
  short_description: "Repo-specific patterns and workflows for frp"
  default_prompt: "Use the frp repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true